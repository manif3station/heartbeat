# EPIC-015

## Title

Package the nested `heartbeat.server` health check.

## Outcome

Make the `server` sub-skill under `heartbeat` behave like a real DD nested command instead of a loose standalone script, with tested JSON output and clear operational documentation.

## Tickets

- `DD-042` Repackage `heartbeat.server.connected`
